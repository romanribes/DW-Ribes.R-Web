@{
	var db = Database.Open("ChistesMalos");
	var sqlSelect = "SELECT * FROM Categorias";    
}

<ul>
@foreach (var categoria in db.Query(sqlSelect))
{
	<li><a href="~/Chistes/@categoria.Nombre">@categoria.Nombre</a></li>
}
</ul>
